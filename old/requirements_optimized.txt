# Optimized Email Intelligence System Requirements
# High-performance dependencies for production deployment

# Core FastAPI and async
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
uvloop>=0.17.0  # High-performance event loop
httptools>=0.6.0  # Fast HTTP parsing

# JSON optimization
orjson>=3.9.0  # Fastest JSON serialization

# Async database
aiosqlite>=0.19.0  # Async SQLite support

# Caching
cachetools>=5.3.0  # In-memory caching with TTL
aiocache>=0.12.0  # Async cache with multiple backends
redis>=5.0.0  # Optional: for distributed caching

# Rate limiting
slowapi>=0.1.8  # Rate limiting for FastAPI

# Performance monitoring
psutil>=5.9.0  # System resource monitoring
prometheus-client>=0.17.0  # Optional: Prometheus metrics

# Compression
python-multipart>=0.0.6  # For file uploads
aiofiles>=23.0.0  # Async file operations

# Existing dependencies
numpy>=1.21.0,<2.0.0
python-dateutil>=2.8.0,<3.0.0
pymongo>=4.6.0  # If using MongoDB
requests>=2.31.0  # For external API calls

# Optional ML enhancements (uncomment as needed)
# scikit-learn>=1.3.0  # For ML models
# joblib>=1.3.0  # For model persistence
# pandas>=2.0.0  # For data processing

# Testing and development
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.24.0  # For async testing